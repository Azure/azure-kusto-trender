apiVersion: v1
kind: Service
metadata:
  name: {{ include "trender.fullname" . }}
  labels:
    {{- include "trender.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "trender.selectorLabels" . | nindent 4 }}